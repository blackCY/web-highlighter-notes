# Web Highlighter Notes 功能说明

> 更新日期：2026-09-12

`Web Highlighter Notes` 是一个 Chrome Manifest V3 网页笔记扩展。它在网页中记录文字、标题和媒体，并将笔记同步到 GitHub 或导出为 Markdown。

## 1. 文字标记与笔记

### 1.1 选择文字后的工具栏

在普通 `http` 或 `https` 网页中选择文字后，会出现网页内浮动工具栏，可创建以下记录：

| 类型 | 网页呈现 | Markdown 呈现 |
| --- | --- | --- |
| 重要 | 有色背景；可选红、绿、橙 | GitHub 笔记中使用深红背景、白字、加粗的 `span` |
| 想法 | 绿色高亮，可填写“我的想法” | 标记下的子项 |
| 疑问 | 蓝色高亮，可填写“我的疑问” | 标记下的子项 |
| 默认笔记 | 灰色背景与细下划线 | 普通摘录 |
| 加粗笔记 | 灰色背景、粗下划线和加粗文字 | 加粗摘录 |
| 标题 H1–H6 | 紫色背景与 H1–H6 气泡，不改变原网页字号 | 对应层级的 Markdown 标题 |

“想法”和“疑问”会打开输入框；按 `Cmd/Ctrl + Enter` 可保存。点击已有文字标记后，可继续修改类型、颜色、权重或添加“备注”。

### 1.2 重叠与重复规则

- 允许记录父段与子段。例如已记录 `AAAA` 后，仍可记录其中的 `AA`。
- 只阻止同一位置、同一范围、同一文本被再次重复记录。
- 历史上重复保存的同一文字范围，在删除其中任意一条时会一次清理全部重复记录。
- 删除父段或子段不会删除另一条不同范围的记录。

### 1.3 网页内气泡与删除

- 每条文字记录会在网页上显示类型气泡，例如“重要”“想法”“默认”“加粗”或“H2”。
- 同一记录可累积显示多个类型气泡。
- 点击已有高亮后会显示“删除笔记”；确认后会删除网页标记、气泡和 GitHub 中对应的记录。
- 删除确认框显示在删除按钮上方，不使用浏览器原生确认弹窗。

### 1.4 右键菜单

在选中文字上点击右键，可通过“保存为网页笔记”选择：

- 重要、想法、疑问；
- 默认笔记、加粗笔记；
- 标题 H1–H6。

## 2. 媒体记录

扩展会识别网页中的图片、视频和音频。

- 鼠标移到媒体上时，会出现“记录此媒体”操作。
- 记录后会保存媒体类型、地址、标签和定位信息，并在媒体左上角显示“已记录”气泡。
- 同一媒体只允许保存一条记录；已记录媒体会显示红色“删除此媒体”操作。
- 如果旧笔记中存在同一媒体的重复记录，点击一次“删除此媒体”会清理全部重复记录，并只保留一枚“已记录”气泡。
- 页面滚动、窗口缩放、刷新和动态内容加载后，媒体气泡会重新定位或恢复。

## 3. GitHub 同步

### 3.1 配置

扩展弹窗右上角的“GitHub”区域可配置：

- 仓库，格式为 `owner/repository`；
- 分支；
- 笔记目录；
- Fine-grained Personal Access Token。

保存配置前会验证仓库和分支。Token 仅保存于当前 Chrome 配置文件的扩展本地存储，不会写入项目、GitHub 笔记或导出的 Markdown。

Token 输入框默认遮挡，支持“显示/隐藏”和“复制”操作。

### 3.2 保存文件规则

- 每个网页对应一份 GitHub Markdown 笔记。
- 文件名默认使用网页标题：`网页标题.md`。
- 若不同网页标题相同，会自动使用 `网页标题-2.md`、`网页标题-3.md`，避免互相覆盖。
- 网页 URL 的查询参数 `?` 和锚点 `#` 不参与页面对应关系；它们会在保存、读取和恢复时被忽略。
- 旧版“标题-哈希”“域名-哈希”“纯哈希”文件仍可读取、删除和自动迁移。

GitHub Contents API 使用的版本 SHA 仅用于安全更新和冲突控制，不会出现在 Markdown 文件名或正文中。

### 3.3 Markdown 数据结构

GitHub 同步的 Markdown 顶部使用 YAML Front Matter：

```yaml
---
title: "网页标题"
source_url: "https://example.com/article"
favicon: "https://example.com/favicon.ico"
updated_at: "2026-09-12T00:00:00.000Z"
---
```

正文包含网页标题、标题层级、文字标记、媒体链接、个人备注等可读内容；同时包含一段隐藏恢复数据，用于将笔记准确恢复到网页中。

### 3.4 同步可靠性

- 新建记录只有在 GitHub 同步成功后才会显示在网页中，避免留下无法管理的假高亮。
- 同一网页的读、写、删除操作在扩展内串行执行。
- GitHub 文件读取禁用缓存；遇到版本 SHA 冲突会重新读取最新内容并最多重试三次。
- 多设备或多标签修改同一网页后，可在扩展弹窗点击“从 GitHub 刷新”拉取最新内容并刷新当前网页。
- 弹窗中的“强制删除”需要二次确认，会删除当前网页的所有新旧命名笔记文件。

## 4. 笔记恢复与动态网页

- 优先用包含 `id` 的页面区域定位文字；没有 `id` 时使用元素类名。
- 文字定位同时保存前后上下文、文本偏移与页面顺序，用于处理同样文本在网页中重复出现的情况。
- 页面正文异步加载或 DOM 变化后，扩展会自动再次尝试恢复尚未显示的文字、标题和媒体记录。
- 若原网页大幅改写、删除了摘录文本或完全改变了 DOM 结构，文字高亮可能无法恢复；GitHub 中的笔记和 Markdown 导出仍会保留。

## 5. 扩展弹窗

扩展弹窗用于浏览和管理当前网页笔记：

- 显示当前页面的文字、标题和媒体记录；
- 按摘录、媒体标题、媒体地址和个人备注筛选；
- 从 GitHub 刷新当前网页笔记；
- 导出当前页 Markdown 到浏览器下载目录，无需本地服务；
- 打开“预览 Markdown”，在当前网页右侧显示可滚动、可关闭的 Markdown 渲染预览；按 `Esc` 也可关闭；
- 对弹窗中的任意文字、标题或媒体笔记点击“定位”，页面会滚动到对应位置并短暂高亮目标；
- 强制删除当前页 GitHub 笔记；
- 配置 GitHub、显示/隐藏 Token、复制 Token。

## 6. 权限与限制

- 支持普通 `http://`、`https://` 网页。
- Chrome 内部页面、Chrome Web Store 等受保护页面不能注入内容脚本，无法使用网页内标记功能。
- GitHub 同步需要网络、有效 Token，以及目标仓库 `Contents` 的读写权限。
- 网页标题是文件名的主要来源；同名网页会自动编号。网页真实对应关系仍以 Front Matter 中的 `source_url` 和隐藏恢复数据为准。
- Markdown 右侧预览以渲染结果展示将要导出的内容，不会直接修改网页笔记或 GitHub 文件。

## 7. 常用操作流程

1. 在 Chrome 打开 `chrome://extensions`，启用开发者模式，并加载本项目目录。
2. 打开扩展弹窗，在“GitHub”中填写仓库、分支、目录和 Token，保存并通过验证。
3. 刷新要记录的网页。
4. 选择文字、使用右键菜单，或移到媒体上创建笔记。
5. 点击扩展图标浏览、筛选、预览 Markdown、导出或从 GitHub 刷新。
6. 如果扩展代码更新，在 `chrome://extensions` 点击“重新加载”，再刷新正在记录的网页。
